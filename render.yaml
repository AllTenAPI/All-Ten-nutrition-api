services:
  - type: web
    name: all-ten-nutrition-api
    env: python
    plan: free
    buildCommand: echo "No build required - using built-in Python libraries"
    startCommand: python app-minimal.py 